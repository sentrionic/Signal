apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: sentrionic/notification-service
          env:
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: session-secret
                  key: SESSION_SECRET
            - name: PORT
              value: "4000"
            - name: RABBIT_MQ_URI
              value: 'amqp://rabbitmq-srv:5672'
            - name: COOKIE_NAME
              value: "sgl"
            - name: DATABASE_URL
              value: 'postgresql://root:password@notification-postgres-srv:5432/signal?sslmode=disable'
            - name: NODE_ENV
              value: 'development'
---
apiVersion: v1
kind: Service
metadata:
  name: notification-srv
spec:
  selector:
    app: notification-service
  ports:
    - name: notification-service
      protocol: TCP
      port: 4000
      targetPort: 4000